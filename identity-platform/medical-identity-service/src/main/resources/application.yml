server:
  port: 8091

spring:
  application:
    name: medical-identity-service
  config:
    import: optional:configserver:http://localhost:8888  # URL du config-service
  cloud:
    config:
      enabled: true
      fail-fast: false