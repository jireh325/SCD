server:
  port: 8102

spring:
  application:
    name: identity-orchestrator-service
  config:
    import: optional:configserver:http://localhost:8888  # URL du config-service
  cloud:
    config:
      enabled: true
      fail-fast: false