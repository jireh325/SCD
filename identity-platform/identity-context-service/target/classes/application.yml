server:
  port: 8082

spring:
  application:
    name: identity-context-service
  config:
    import: optional:configserver:http://localhost:8888  # URL du config-service
  cloud:
    config:
      enabled: true
      fail-fast: false