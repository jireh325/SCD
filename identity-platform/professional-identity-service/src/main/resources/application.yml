server:
  port: 8092

spring:
  application:
    name: professional-identity-service
  config:
    import: optional:configserver:http://localhost:8888  # URL du config-service
  cloud:
    config:
      enabled: true
      fail-fast: false